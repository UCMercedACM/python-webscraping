# Import libraries

# Make sure user provided enough command line arguments

# Parse inputs from command line - name, gender, and state

# Choose what file to retrieve, and where to put it

# Create destination directory if it doesn't exist

# Check if we already have the file

	# If not, download it

	# File is in zip format, so unzip it to use contents

# At this point, we have a directory of files, one for each year. let's list them

# Get name of the file we want

# Read data from file

# Step through data, store year and number of people

# If we have data, plot it

# Otherwise, return an error
